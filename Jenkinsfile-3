pipeline {
  agent { dockerfile true }
  stages {
    stage('Tooling') {
      steps {
        sh '''
          node --version
          git --version
          curl --version
          syft --version
        '''
      }
    }
  }
}
